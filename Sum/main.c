#include <stdio.h>
int main()

{
    int sum;

    sum = 25;
    printf("\nThe number stored in sum is %d.", sum);
    sum = sum +10;
    printf("\nThe number now stored in sum is %d.\n", sum);

    return 0;
}
