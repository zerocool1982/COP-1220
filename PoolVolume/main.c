//This program calculates the volume of a pool

#include <stdio.h>
int main(void)
{
    int length = 25;
    int width = 10;
    int depth = 6;
    int volume = length * width * depth;

    printf("The volume of the pool is %d", volume);

    return 0;
}
