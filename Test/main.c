/* page 113 problem 1 */

#include <stdio.h>
int main()
{
    int length, width, area;

    width = 15;
    length = 20; /* must be assigned some value */

    area = length * width;
    printf("The area is %d", area);

    return 0;
}
