#include <stdio.h>
int main()

{
    int sum;

    sum = 0;
    printf("\nThe value of sum is initially set to %d.", sum);
    sum = sum + 96;
    printf("\nsum is now %d.", sum);
    sum = sum + 70;
    printf("\n sum is now %d.", sum);
    sum = sum + 85;
    printf("\n sum is now %d.", sum);
    sum = sum + 60;
    printf("\nThe final sum is %d.\n", sum);

    return 0;
}
